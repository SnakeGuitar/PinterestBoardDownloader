<Window x:Class="PinterestBoardDownloader.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PinterestBoardDownloader" xmlns:local1="clr-namespace:PinterestBoardDownloader.Model"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Aero Pinterest Downloader" Height="730" Width="1000"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button" x:Key="AeroButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#20FFFFFF"/>
            <Setter Property="BorderBrush" Value="#60FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#80FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#10FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#80FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AeroComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#10FFFFFF"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" CornerRadius="6" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                            <Path x:Name="Arrow" Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#80FFFFFF"/>
                                                <Setter TargetName="border" Property="Background" Value="#20FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="10,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left" />

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder" Background="#E6002A3A" BorderThickness="1" BorderBrush="#40FFFFFF" CornerRadius="6" Margin="0,2,0,0">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="4" Margin="2,1">
                                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#30FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#50FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#001F3F" Offset="0"/>
                <GradientStop Color="#004d4d" Offset="0.5"/>
                <GradientStop Color="#008080" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="16" Margin="0,0,0,25" 
                    Background="#20FFFFFF" BorderBrush="#40FFFFFF" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.25" Color="Black"/>
                </Border.Effect>

                <StackPanel Margin="25">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,15">
                        <TextBlock Text="🔗" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="URL del Tablero" Foreground="White" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBox Text="{Binding BoardUrl}" Height="42"/>

                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0" VerticalAlignment="Center">
                        <TextBlock Text="🛑" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Detener al llegar a:" Foreground="White" VerticalAlignment="Center" FontWeight="Medium" FontSize="15"/>
                        <TextBox Text="{Binding MaxPins}" Width="70" Margin="15,0,0,0" Height="30" 
                                 Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="15"/>
                        <TextBlock Text="imágenes" Foreground="#CCFFFFFF" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="15"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                        <TextBlock Text="⚙️" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Modo:" Foreground="White" VerticalAlignment="Center" FontWeight="Medium" FontSize="15" Margin="0,0,10,0"/>

                        <ComboBox SelectedItem="{Binding SelectedMode}" Style="{StaticResource AeroComboBox}" Width="233" Height="30">
                            <sys:String>Fast_Limit</sys:String>
                            <sys:String>Strict_BoardOnly</sys:String>
                        </ComboBox>
                    </StackPanel>

                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📁" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SavePath}" Height="35" IsReadOnly="True" 
                                      Background="#15FFFFFF" BorderBrush="#30FFFFFF" Foreground="#DDFFFFFF" Width="454"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                            <Button Content="Examinar" Width="90" Height="35" Style="{StaticResource AeroButton}" Click="OnBrowseFolder"/>

                            <Button Content="▶ START" Width="90" Height="35" Margin="10,0,0,0" 
                                    Style="{StaticResource AeroButton}" Click="OnStart" Background="#3000C2FF" BorderBrush="#6000C2FF"/>

                            <Button Content="⏸" Width="45" Height="35" Margin="10,0,0,0" ToolTip="Pausar"
                                    Style="{StaticResource AeroButton}" Click="OnPause" FontSize="16"/>

                            <Button Content="⏯" Width="45" Height="35" Margin="5,0,0,0" ToolTip="Continuar"
                                    Style="{StaticResource AeroButton}" Click="OnContinue" FontSize="16"/>

                            <Button Content="⏹" Width="45" Height="35" Margin="5,0,0,0" ToolTip="Detener"
                                    Style="{StaticResource AeroButton}" Click="OnStop" FontSize="16" Background="#30FF0000" BorderBrush="#60FF0000"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#10FFFFFF" CornerRadius="16" Padding="15" BorderBrush="#20FFFFFF" BorderThickness="1">
                <ScrollViewer BorderThickness="0" BorderBrush="Aqua">
                    <ItemsControl ItemsSource="{Binding ProcessedFiles}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="150" Height="190" Margin="8" Background="#20FFFFFF" CornerRadius="10" BorderBrush="#40FFFFFF" BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15" Color="Black"/>
                                    </Border.Effect>
                                    <StackPanel Margin="5">
                                        <Border CornerRadius="8" ClipToBounds="True">
                                            <Image Source="{Binding Url}" Height="150" Stretch="UniformToFill"/>
                                        </Border>
                                        <TextBlock Text="{Binding FileName}" FontSize="11" Foreground="White" 
                                                   HorizontalAlignment="Center" Margin="0,6,0,2" TextTrimming="CharacterEllipsis" ToolTip="{Binding FileName}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5,15,0,0" VerticalAlignment="Center">
                <TextBlock Text="ℹ️" Foreground="White" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="13" FontWeight="Medium"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>